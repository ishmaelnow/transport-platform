# Deployment Environments

This directory is reserved for environment-specific deployment overlays.

Keep environment configuration reviewable and non-secret. Secrets should be referenced from the runtime secret store, not committed here.
