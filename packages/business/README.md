# Business Packages

This directory is reserved for shared transportation business modules.

Business packages should be introduced only when a transportation capability needs reusable domain logic across applications or services. Each package should have a clear domain boundary and should avoid depending on application-layer code.

No business modules are defined yet.
