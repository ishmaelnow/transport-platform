# Tooling

This directory contains developer automation.

- `scripts/` is for reusable local or CI automation.
- `generators/` is for scaffolding new modules consistently.
- `templates/` is for shared templates used by generators or documentation.

Tooling should reduce repeated decisions while preserving clear ownership boundaries.
