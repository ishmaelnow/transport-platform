# Environment Configuration

Committed non-secret environment configuration templates can live here.

Use this directory to document required variables and safe defaults once applications and services are introduced.
