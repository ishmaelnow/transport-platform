# Configuration

This directory contains versioned, non-secret configuration templates and defaults.

Configuration here should be safe to commit. Secrets, credentials, access tokens, and tenant data must not be stored in this repository.

Use this directory for shared runtime defaults, environment templates, feature flag shape definitions, and configuration documentation once the platform stack is selected.
