# Terraform

Terraform modules and stacks can live here if Terraform is selected for infrastructure provisioning.

Keep reusable modules separate from environment-specific stacks when they are introduced.
